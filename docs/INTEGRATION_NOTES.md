# Integration notes

The only cross-agent channel. One line per request, format: `[HH:MM] [agent] [path] [what you need, one sentence] [blocking: yes|no]`.
The integrator applies or reassigns each line; do not edit files you do not own.

[10:55] [scaffold] [backend/app/main.py] `GET /api/health` lives permanently in main.py, not core/sim.py; backend-core must not redefine it (BUILD_PLAN §1.2 / §4.12 amended by orchestrator). [blocking: no]
[10:55] [scaffold] [backend/requirements.txt] Added `greenlet==3.5.5`; SQLAlchemy async raises without it and BUILD_PLAN's list omitted it. [blocking: no]
[10:55] [scaffold] [backend/app/alerts/bands.py] `band_key` body is implemented (CONTRACT §5.1 rule) alongside DEFAULT_POLICY; backend-alerts may keep or rewrite it, signature fixed. [blocking: no]
[10:55] [scaffold] [backend/app/main.py] Routers are mounted via importlib over ROUTER_MODULES; a module without `router` is skipped with a warning, so an implement agent can never break boot by omitting it. [blocking: no]
[10:55] [scaffold] [backend/pytest.ini] `asyncio_mode = auto`; tests need no `@pytest.mark.asyncio`. [blocking: no]
[10:55] [scaffold] SCAFFOLD A READY
[11:00] [submission-docs] [Makefile] `make install` assumes backend/.venv exists; on a clean clone it does not (gitignored), so README tells users to run `uv venv backend/.venv --python 3.13` first; consider adding that as the first line of the install target so `git clone && make install && make dev` works as written. [blocking: no]
[11:07] [hardware-docs] [hardware/schematic.svg] Done: 1600×1000, no external refs, DESIGN colours; fe-hardware-3d can copy it to frontend/src/assets/schematic.svg or import it from hardware/ (I do not touch frontend/). 2× raster at docs/schematic.png (3200×2000, headless Chrome; rsvg-convert not installed). [blocking: no]
[12:00] [backend-core] [backend/app/models.py] `Incident` has no `mesh` column, so `Incident.mesh` (CONTRACT §3.1) is derived on read from the latest `mesh_log` row whose payload.code matches (`core.mesh.incident_mesh`); no schema change needed, noting so nobody adds one twice. [blocking: no]
[11:20] [backend-drills] [backend/app/alerts/ingest.py] GET /api/timeline acceptance (readings>0, decisions>=1) needs readings and decision_log rows that only backend-alerts ingest writes; verified with a seeded full day (8 nodes x 1560 ticks, 40-70 ms over HTTP), re-run the curl once ingest lands. [blocking: no]
[11:15] [simulator] [backend/app/alerts/ingest.py] Simulator posts one telemetry batch at startup and after every jump even while paused (playing:false), and sends sim-state every 1 s; ingest should accept readings regardless of play state. At 11:12 the route still returned 404 (stub), so the telemetry half of §2.4 acceptance waits on backend-alerts. [blocking: no]
[12:10] [fe-admin] [frontend/src/components/ui/Segmented.tsx] Segmented has a fixed h-9; the Overview map panel header passes className="h-7" to fit the 40px header. If it renders too tall, allow a size prop or let className win. [blocking: no]
[12:10] [fe-admin] [frontend/src/components/map/CampusMap.tsx] highlightCode resolves the node via hops[].payload.code -> origin_node; consumers wanting a pulse on an incident node must pass hops that include that incident's relay entries (fe-responder: pass useMeshStore hops). [blocking: no]
[12:10] [fe-admin] [verification] Runtime check of /admin via the shared Playwright browser was inconclusive: other agents navigated the same tab mid-check. Backend + sim booted; GET /api/sites/1/overview returned 8 nodes, 10 links; frontend tsc and vite build pass. [blocking: no]
[12:10] [backend-alerts] [backend/app/core/nodes.py] ingest calls `node_status_payload(session, node_id)` after every accepted reading and broadcasts `node_status` only when status/band/open kinds change; while the stub still raises NotImplementedError the events are suppressed with one warning, so backend-core landing it is all that is needed (no change requested). [blocking: no]
[12:10] [backend-alerts] [backend/app/alerts/routes.py] decision-card, air, sms/outbox, nodes/{id}/explain and wea/draft are sub-routers included from alerts/routes.py, so ROUTER_MODULES in main.py needs no change. [blocking: no]
[12:10] [backend-alerts] [backend/tests/test_drills.py] `test_timeline_decimates_in_sql_and_is_fast` failed once in a full `pytest -q` run on my machine (timing assertion while other tests ran); backend-drills may want a looser bound. [blocking: no]
[11:35] [fe-landing] [frontend/src/features/landing/HeroCard.tsx] Hero card draws its own hop dash in an SVG overlay (viewBox 0 0 1000 700, same as topology.json) and does not pass `hops` to CampusMap, so the landing shows the signature dash with no backend; fe-admin must keep CampusMap's viewBox at 0 0 1000 700 or the overlay misaligns. [blocking: no]
[11:35] [fe-landing] [frontend/src/components/ui/StatStrip.tsx] Frozen StatStrip truncates "3 days" in a half-width cell at 390px, so the landing passes value "3" with label "days on one 18650, no sun" instead of DESIGN §8.1's "3 days". [blocking: no]
[11:35] [fe-landing] [frontend/src/components/SafetyFooter.tsx] SafetyFooter takes optional `repoUrl` / `devpostUrl` props; "Repo" and "Devpost" render as plain text until submission-docs supplies the URLs (no repo or Devpost URL exists in any doc yet). [blocking: no]
[11:35] [fe-landing] [frontend/src/features/landing/HeroCard.tsx] Landing renders its own LIVE dot instead of ui/LiveDot because LiveDot reads the WebSocket store and would show RECONNECTING on a page that never opens a socket. [blocking: no]
[11:25] [fe-responder] [frontend/src/components/shell/RequireRole.tsx] Any full reload with a stored token hangs on the loading skeleton: `me().then(setAuth)` changes `role`, which reruns the effect and sets `cancelled=true` before `.finally` runs, so `setChecking(false)` never fires; fix by calling `setChecking(false)` inside `.then` before `setAuth`, or dropping the `cancelled` guard in `.finally`. [blocking: yes]
[11:26] [fe-drill] [frontend/src/components/shell/RequireRole.tsx] Fresh page load with a stored token but no role sticks on the Loading skeleton forever (StrictMode + zustand: `setAuth` inside `.then` changes `role`, the effect cleanup sets `cancelled=true`, so the `.finally` never calls `setChecking(false)`); every console URL opened directly or reloaded, plus `/drills/:id/report` in a new tab, hits this. Fix: call `setChecking(false)` inside `.then` before `setAuth`, or derive `checking` as `Boolean(token) && !role` instead of state. [blocking: yes]
[11:26] [fe-drill] [backend/app/deps.py] `GET /api/export/drill/{id}.csv` needs a bearer but a plain `<a href download>` cannot send one (verified 401); fe-drill works around it by fetching with the header and saving a blob. If `_bearer` also accepted `?token=` the CONTRACT's plain-link download would work as written. [blocking: no]
[11:26] [fe-drill] [frontend/src/components/shell/PhoneShell.tsx] The "Which node are you next to?" dialog also opens on `/m/staff`; teachers pick a muster point inside the roll call form, so consider skipping the picker on that path. [blocking: no]
[12:02] [fe-phone] [frontend/src/components/ui/CodeCells.tsx] Phone size at 56px per cell overflows the 350px content column at 390 wide (2+4 cells, gaps, hyphen ≈ 390px); CodeScreen narrows cells to 48px via [&>span]:w-12! locally, consider making the phone cell 48px in the shared component. [blocking: no]
[12:08] [fe-hardware-3d] [frontend/src/components/SafetyFooter.tsx] Its event line "Frontier Cascadia" is the only "Cascadia" on /hardware; BUILD_PLAN §2.10 acceptance says the word must be absent. Either the acceptance means "Cascadia Node" (product name, which is absent) or fe-landing should render the event line differently; I did not touch the footer. [blocking: no]
[12:08] [fe-hardware-3d] [frontend/src/store/site.ts] /hardware is public (no shell), so it seeds `useSiteStore.setState({ nodes })` from GET /api/nodes?site_id=1 when `loaded` is false, so live reading/node_status events colour the mesh pillars; no file edit, noting the out-of-band setState so nobody is surprised. [blocking: no]
[12:08] [fe-hardware-3d] [backend/app/core/mesh.py] GET /api/mesh/log requires a bearer token, so the /hardware hop log starts empty for anonymous visitors and fills from the WS `hop` stream (which anonymous clients do receive); making the log public would let the page show history on first paint. [blocking: no]
[11:50] [fe-novel] [frontend/src/lib/types.ts] `WeaDraft` lacks `cap_xml`, `eligible`, `eligibility_note`, `vertex_count` that backend `alerts/wea.py` returns; WeaDraftModal types them locally as optional (`WeaDraftFull`), so adding them to `WeaDraft` is a no-op cleanup. [blocking: no]
[11:50] [fe-novel] [backend/app/alerts/engine.py] After `jump` rewinds the sim clock (20:00 -> 14:30) `/api/nodes/gym/explain` keeps `at: 20:00` and never reaches LOCAL_FIRE (temp_rise stays negative); the engine seems to drop readings older than its last eval, so the fire acceptance (drawer shows LOCAL_FIRE, Draft WEA enabled) needs a fresh day or an engine reset on jump. Drawer and modal verified with LOCAL_SMOKE_SUSPECT and a mocked FRW draft. [blocking: no]
[11:50] [fe-novel] [backend/app/alerts/engine.py] LOCAL_SMOKE_SUSPECT alert `reason` text contains an em dash ("no heat — ask staff"); it surfaces verbatim in the banner and pin hover card, DESIGN §0.11 bans em dashes in UI copy. [blocking: no]
[11:50] [fe-novel] [frontend/src/store/timeline.ts] Time Machine 1x = 60 sim seconds per real second (the simulator's default pace), 10x = 600; the scrub ceiling is the latest stored reading, and `TimeMachine` calls `goLive()` on unmount so leaving /admin never leaves a stale frame under `select.ts`. [blocking: no]
